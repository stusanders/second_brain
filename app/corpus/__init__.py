"""Corpus pipeline (docs/MVP_SPEC.md): one-shot document corpus → wiki + map
+ frontier. Six stages; the page set is fixed (Stage 3) before any page is
written (Stage 4), so [[links]] can only target pages that will exist."""
